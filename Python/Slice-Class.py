tags = [
  'python',
  'development',
  'tutorials',
  'code',
  'programming',
]

#print(tags[:2])

slice_obj = slice(1, 2, 3)
# We can only do slice_obj = slice (5) and it will bring all the items.



print(slice_obj.start)
print(slice_obj.stop)
print(slice_obj.step)
print(slice_obj)
print(tags[slice_obj])

# This slice function can be used at anytime you would like to define ranges.....

# You can make one change instead of getting one by one.
